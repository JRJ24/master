Create table Users(
    ID_User NUMBER(10)
    CONSTRAINT ID_User_PK PRIMARY KEY,
    userName VARCHAR2(25)
    CONSTRAINT userName_NN NOT NULL,
    passwordUser VARCHAR2(20) 
    CONSTRAINT password_nn NOT NULL
);

Alter table CONTACTOS
    ADD ID_User NUMBER(10);

Alter table CONTACTOS
    ADD CONSTRAINT ID_User_FK FOREIGN KEY (ID_User) REFERENCES Users(ID_User);

Create SEQUENCE ID_CONTACT_SE
    START WITH 1
    INCREMENT BY 5
    NOMAXVALUE
    NOCYCLE
    CACHE 1000;

SELECT ID_CONTACT_SE.NEXTVAL FROM DUAL;

Create SEQUENCE ID_USER_SEQ
START WITH 1
INCREMENT BY 5
NOMAXVALUE
NOCYCLE
CACHE 1000;

SELECT ID_USER_SEQ.NEXTVAL FROM DUAL;


SELECT * FROM CONTACTOS;
